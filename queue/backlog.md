# Backlog — external PR candidates for human review

## 2026-08-13
- **No confident external typo/doc target found this pass.** Swept openai-cookbook, langchain, langgraph, vllm, transformers, ollama, litellm, autogen, crewAI, llama_index, haystack, AutoGPT, pytorch, mcp/servers for common misspellings (recieve/seperate/compatability/occured/paramter/arguement/lenght/sucessful/existant/definately/occurence/retreive) in markdown — all clean. Well-maintained repos have already been swept by others.
- Lead worth a human pass: `crewAIInc/crewAI` returns 98 `the the` matches in .md — mostly sentence-boundary false positives ("...the. The...") but may hide 1-2 real duplicated-word typos. Needs eyeballing, not blind fix.
- Next time: target younger / fast-moving trending AI repos and their `examples/` (runnable snippets that drift out of date) rather than flagship docs; or filter GitHub `good first issue`+`documentation` more broadly instead of per-org.
## 2026-08-16 — fetchai/uAgents (~1.6k stars) — PRed as #934, resolved
- Was verified KeyError in LangChain adapter README; shipped as fetchai/uAgents#934 on 2026-08-16. No longer a backlog item.

## 2026-08-16 — dynamiq-ai/dynamiq (~1.1k stars, verified, candidate for future)
- README.md async ReAct example ends with asyncio.run(...) (L142) but never imports asyncio (only imports at L101-104) -> NameError on copy-paste.
- Fix: add one line  to that snippet import block.
- Was opened as dynamiq#898 then closed same day to keep to a 1-PR/day cap (uAgents#934 took the slot). Branch feizhuzheng:docs/readme-add-missing-import-asyncio still has the change; can reopen/resubmit on a future day.

## 2026-08-21 · DeepSeek 官方 org 方向（已独立核实仓库真实、活跃、开 issues）
高价值目标，B 段优先考虑（拿"DeepSeek 官方/生态 merged PR"身份）：
- **deepseek-ai/awesome-deepseek-integration** ⭐38.8k（官方 deepseek-ai org，开 issues）——README 收录/格式/失效链接/typo 类，合一个=官方组织内 merged PR。**注意**：awesome 列表 PR 常要求按 CONTRIBUTING 的条目格式提交，先读它的规范。
- **anywhere-labs/deepseek-harness-desktop** ⭐16.5k（TS 桌面 harness，开 issues）——输出截断/filesize 格式化/显示宽度类 bug，跟 crush#16451、cline#11999 的 UTF-8/emoji 截断同型，命中率高。
- **awesome-dsh-plugin/awesome-dsh-plugin** ⭐10.6k（收录列表，开 issues）——docs/i18n 不一致。
- 主仓 deepseek-ai/deepseek-harness ⭐17万 **issues 关闭、不收外部 PR**，别在主仓浪费子弹；长线可在 Discussions 发高信号 bug 报告卡位。
（来源：一次生态调研，已用 GitHub API 逐个核实 star/issues/archived 属实。找到真问题再提，仍守质量门槛。）

## 2026-08-21 · sweep notes (B done: awesome-dsh-plugin#2495 shipped)
- **anywhere-labs/deepseek-harness-desktop** ⭐16.6k — swept src (truncation/byte-budget/format): exceptionally defensive (byteLength everywhere, Array.from for code-point-safe slicing, correct diagnostic byte-budget accumulation). No bug found this pass; 60 md files, 0 broken relative links. Low hit-rate for our usual truncation angle — deprioritize.
- **deepseek-ai/awesome-deepseek-integration** ⭐38.9k (official org, last push 2026-02-23) — README docs refs all resolve (53/53), no common typos. Clean; would need a dead external-link pass (network HEAD) to find anything, and it's heavily swept. 
- **awesome-dsh-plugin/awesome-dsh-plugin** ⭐10.7k — data-driven generated list; found+fixed a real data-integrity bug (plugin file missing .yml extension -> silently dropped) = PR #2495. Future angle here: run `node scripts/generate-readme.mjs --check` for staleness, and scan data/plugins/*.yml for unquoted ': ' in description.en (contributing.md's known parser trap).

## 2026-08-22 · mem0ai/mem0 — pr-gate `isDocs` too narrow (real 2nd bug, future target)
- `.github/workflows/pr-gate.yml` L56: `isDocs = (filename) => filename.startsWith('docs/') || rootDocs.has(filename)`. Only `docs/**` + root README/CONTRIBUTING/CODE_OF_CONDUCT/SECURITY count as docs. Markdown docs living under `skills/**`, `examples/**`, `cookbooks/**` (e.g. our #7061 edit to `skills/mem0/references/integration-patterns.md`) are NOT recognized, so genuine documentation-only PRs get auto-closed even though policy states "Documentation-only changes skip this gate entirely."
- Fix idea: broaden `isDocs` to also treat `*.md`/`*.mdx` files (or add `skills/`, `examples/`, `cookbooks/` prefixes) as docs. BUT this edits a workflow (non-docs) so it would itself be gated -> needs an `accepted` issue first. Don't blind-PR; note for when we're a vouched/accepted contributor.
- #7061 (relative-link fix) currently auto-closed by this same gap; commented asking for reopen.

## 2026-08-25 · livekit/agents top-level README — ~8 dead example links (verified, NOT PR'd)
Verified against livekit/agents default branch: the README "Examples" section links to files deleted by the July examples-revamp (PR #5523 "remove redundant files", merged 2026-07-13). None exist anywhere in the repo now:
- L149 `examples/voice_agents/multi_agent.py` (revamp reason: "favor examples and restaurant agent")
- L257 `examples/voice_agents/push_to_talk.py`
- L267 `examples/voice_agents/background_audio.py` (revamp: now docs-only)
- L274 `examples/voice_agents/dynamic_tool_creation.py`
- L291 `examples/voice_agents/structured_output.py`
- L335 `examples/voice_agents/restaurant_agent.py`
- L308 `examples/other/text_only.py`
- L325 `examples/avatar_agents/` (revamp: documented on plugin pages)
Why not PR'd this round: no verifiable 1:1 replacement — the examples were consolidated into docs / removed, and are NOT in the external `livekit-examples/python-agents-examples` repo (code-searched: 0 hits for push_to_talk/structured_output/dynamic_tool_creation/multi_agent/restaurant). Repointing each link (to docs URL vs external repo vs section removal) is maintainer-judgment, so a mass-rewrite would be guesswork = spam risk. Better handled as a GitHub issue, or a PR only after confirming intended targets with maintainers. High visibility (main README, ~13k-star repo) so worth doing once targets are known. Repo has NO PR title-lint/changeset gate; CLA bot on first PR (feizhuzheng already opened #6972 there this run).

## 2026-08-26 · pipecat sweep (B done: pipecat-ai/pipecat#5439 shipped)
- **B shipped**: pipecat-ai/pipecat#5439 — examples/README.md linked `./thinking-and-mcp/` (dead dir); examples were split into `examples/thinking/` + `examples/mcp/` (both exist). Split the stale section into two resolving entries. Docs-only → no changelog fragment (CONTRIBUTING exempts docs); no PR-title lint. New repo (not in prior skip list), new type (stale doc link to split/renamed example dir).
- **Secondary pipecat finding, NOT PR'd (same run, keep 1-PR/day)**: `examples/flows/assets/hold_music/README.md` links `[warm transfer example](../warm_transfer.py)` → from `assets/hold_music/` that resolves to `examples/flows/assets/warm_transfer.py` (missing); real file is `examples/flows/warm_transfer.py`, i.e. link should be `../../warm_transfer.py`. Trivial one-char-family fix but it's the SAME broken-relative-link type as mem0#7061, so deferred to avoid repeating a type. Good easy future pickup.
- **camel-ai/camel**: docs/mintlify/reference/index.mdx etc. use Mintlify absolute paths (`/reference/...`, `/images/...`) — filesystem-broken but Mintlify-served, FALSE POSITIVES, do not "fix". Typos clean. browser-use clean. Deprioritize link-audits on Mintlify-based docs.

## 2026-08-28 · VoltAgent sweep (B done: VoltAgent/voltagent#1408 shipped)
- **B shipped**: VoltAgent/voltagent#1408 — new repo (10.4k stars, active) + reused dead-link type but a different sub-case (removed example, not renamed): examples/README.md listed `[SDK Trace Example](./sdk-trace-example)` but that dir was deleted in 5aa84b5b (feat: add evals #674), 404s on GitHub, no replacement example → removed the stale line. Gates: commitlint (config-conventional) = used `docs(examples):`; Biome doesn't lint md; no changeset gate for non-package examples doc. First-time-contributor CI awaits maintainer workflow-approval (normal).
- **Secondary VoltAgent, NOT PR'd (kept 1-PR/day)**: same dead `sdk-trace-example` ref also in `website/blog/2025-10-21-ai-agent-examples/index.md:265` (full GitHub tree URL, 404). Left alone — dated blog post, point-in-time content; noted in PR body. Future micro-fix if maintainers want it.
- **fast-agent (evalstate/fast-agent, 3.9k stars) codespell**: only weak hits, all FALSE POSITIVES — config.py `SHTTP` (intentional Streamable-HTTP abbrev), oauth_client.py `asend` (real async-gen method), plugins.py `unparseable` (valid word). Docs are Docusaurus (trailing-slash route links) → link-audit = false positives, deprioritize. No clean target found.
- **VoltAgent guardrails/defaults.ts `cunt`** = intentional profanity blocklist, NOT a typo. Do not "fix".

## 2026-08-29 · sweep notes (B done: microsoft/agent-framework#7942 shipped)
- **B shipped**: microsoft/agent-framework#7942 (13.2k stars, active, merges community PRs) — 4 Python-side typos (Conector/targetted/pacakges + _handoff.py docstring "Depending of wether"->"Depending on whether"). Type = typo cluster (deliberately NOT broken-link this round).
- **agent-framework future .NET PR (verified, NOT yet PR'd)**: genuine typos in dotnet/src XML-doc-comments — ChatHistoryProvider.cs:20 `messsages`->messages, FunctionInvocationDelegatingAgentBuilderExtensions.cs:34 `pipline`->pipeline, WorkflowActionVisitor.cs:260 `// Perpare`->Prepare, ResponseAgentProvider.cs:28 `corrsponding`->corresponding, DeviceMetadata.cs:8 `Metdata`->Metadata (verify it's a comment not a type name before touching). Also active docs (not ADRs): could bundle. Keep as a separate scoped ".NET:" PR on a future day (different area, avoids one giant polyglot diff). Skip the docs/decisions/*.md ADR typos (archival decision records) and CHANGELOG.
- **DEAD ENDS (do not target — verified this pass)**: `lastmile-ai/mcp-agent` (8.5k stars) real typos (simiarity x2 docstrings, intialized, tranformed CLI help) BUT last default-branch commit 2026-01-25 and a batch of legit fix PRs (681/680/734/735/736/737) all CLOSED-UNMERGED in Aug = contributions declined/frozen. `humanlayer/humanlayer` (11.3k) real typo (router/index.ts "onthe") BUT 10/10 recent closed PRs unmerged = dev moved off this repo. Don't spend bullets there.
- **AVOID**: `strands-agents/sdk-python` — has 2 clean source typos (loader.py docstring `atributes`, litellm.py comment `mulitple`) but CONTRIBUTING explicitly says "you are the author of your PR, not your agent" + `.github/workflows/bot-pr-review-gate.yml` requires 2 maintainer approvals for bot/agent-authored PRs. High rejection/friction risk; deprioritize.
- **False positives logged (don't "fix")**: ag2 input_events.py `.iif` = Intuit file-extension MIME map; mcp-agent utils.py `"complet" in status_lower` = intentional prefix substring match (same for `terminat`); letta PRIVACY.md `**R**ecognized` = markdown-bolded R (not "ecognized"); mastra "Depositing rime fog" = WMO weather term; goose agent.rs `fot`/`pres` = variable names.

## 2026-08-30 · B sweep = 0 (no defensible non-typo fix found; verified-clean, not spam)
Target type today: doc-code inconsistency / broken example (NOT typo — yesterday was typo). Swept 4 fresh repos not in prior skip list; all either airtight or hostile to AI-agent PRs. Shipped nothing per "宁可 0 不刷垃圾".
- **openai/openai-agents-python** (29k stars, IDEAL venue: no CLA, no CONTRIBUTING gate, no title-lint, no anti-AI, docs not CI-executed) — ran 4 mechanical checks, ALL clean/false-positive:
  - doc `from agents import X` vs `__all__`: only hit `AsyncOpenAI` (docs/models/index.md:328,357) but it's bound in `agents/__init__` via `from openai import AsyncOpenAI`, so `from agents import AsyncOpenAI` WORKS at runtime — stylistically odd (should import from `openai`) but not a bug, don't "fix".
  - submodule doc imports (`from agents.extensions import handoff_filters`): resolves (handoff_filters.py is a real submodule).
  - example-file `from agents import SQLiteSession` (7 files): resolves via PEP-562 lazy `__getattr__` in `agents/__init__` (in `__all__` line 465). NOT broken.
  - all `examples/...` path refs in docs exist; all `[..][agents.X.Y]` autorefs resolve. AIRTIGHT. Good future venue — recheck after a big rename lands.
- **pydantic/pydantic-ai** (19.6k, no CLA; pr-guard.yml auto-closes external PRs w/o linked issue UNLESS docs-only `docs/**`|`*.md` — so a docs-only fix is the only viable drive-by, and CONTRIBUTING welcomes trivial doc fixes). BUT repo runs its OWN AI docs-drift + bug-hunter agents and lychee-on-PR (offline, `--include-fragments`) so links+anchors are CI-caught. Verified ~8 prose "defaults to `X`" claims (retain_images_max=100, CAMERA_TURN_COVERAGE=all_input, DONT_LOG defaults, realtime-camera env vars) — ALL correct. 39 autoref "misses" all FALSE POSITIVES (my resolver doesn't descend into class attrs / optional anthropic+bedrock modules / bare-module refs; every symbol exists). Docs airtight. If a real docs-only drift appears here it's a clean no-CLA PR — worth periodic recheck.
- **stanfordnlp/dspy** (37.6k, active) — AVOID: CONTRIBUTING.md L159 "Do not submit issues, PRs, or reviews from fully autonomous AI agents (e.g. OpenClaw)." Explicit anti-AI-agent stance (same reason strands-agents was avoided). Also 0 broken doc links found anyway.
- **getzep/graphiti** (30.4k, active) — high friction: cla.yml (CLA gate) + ai-moderator.yml (AI PR moderator). Didn't deep-sweep; deprioritize unless a strong fix appears.
- **BerriAI/litellm** (57.5k) — CLA-gated + docusaurus website docs (docs/my-website) NOT present in default shallow checkout, so couldn't sweep the main doc corpus this pass; conventional-commits + close_low_quality_prs.yml gates. Revisit with a full (non-shallow / correct-path) checkout — litellm provider docs are historically drift-prone and a docs PR needs no tests.
- Method note: static AST import/autoref checks over these dynamic-export Python SDKs have a HIGH false-positive rate (PEP-562 `__getattr__` lazy exports, `from openai import X` re-binds, optional-provider modules, bare-module autorefs). Always hand-verify a flag against source before treating as a bug — every "hit" this round was a FP.

## 2026-08-31 · opensre sweep (B done: Tracer-Cloud/opensre#5878 shipped) + candidates
- **B shipped**: Tracer-Cloud/opensre#5878 (~10.9k stars, AI-SRE framework, daily pushes) — wrong section anchor: docs/tool-placement-policy.md:6 pointed at ARCHITECTURE.md#tier-2--tools-and-integrations but that content is Tier 3 (Tier 2 = bootstrap); fixed tier-2->tier-3. New repo + new type (anchor tier mismatch, not dead-link/typo). Conventional `docs:` title, PR template filled, no accepted-issue gate.
- **opensre README.md:56 (candidate, verified)**: TOC item `- [Contributing & development](#contributing--development)` is a DANGLING anchor — no such heading anywhere in README and no CONTRIBUTING.md. Separately README.md:260 has a real `## Integrations` section that is MISSING from the TOC. Fix is maintainer-judgment (delete the dangling item vs add the section; add Integrations to TOC), so kept out of the single-char PR. Good future pickup once intent is clear.
- **OpenHands/software-agent-sdk clients/typescript/AGENTS.md:280 (candidate, verified)**: relative link `[.github/workflows/README-RELEASE.md](.github/workflows/README-RELEASE.md)` resolves from clients/typescript/ to clients/typescript/.github/workflows/README-RELEASE.md (404); real file is at repo root .github/workflows/README-RELEASE.md → fix URL to `../../.github/workflows/README-RELEASE.md` (display text unchanged). NOTE this repo has pr-description-check + review-thread-gate = higher friction than opensre; hold for a day we want to spend the friction.
- **(weak, unverified)** opensre infrastructure/deployment/ec2/telegram_gateway/README.md links `../../../.env.deploy.example` (likely gitignored env-example, low value) — not pursued.
- **Clean this pass (0 real bugs, deprioritize)**: awslabs/cli-agent-orchestrator, awslabs/aidlc-workflows, hashgraph-online/hol-guard.
- **Heavy-gate skips (don't spend bullets)**: deepagents, omnigent (require-issue-link / maintainer-approval-before-CI / anti-bot).

## 2026-09-01 · firecrawl sweep (B done: firecrawl/firecrawl#4474 shipped) + candidates
- **B shipped**: firecrawl/firecrawl#4474 (~174.9k stars, merges community PRs, no gates) — broken relative link: apps/test-suite/README.md:59 `../CONTRIBUTING.md` resolves to apps/CONTRIBUTING.md (404); root has the only CONTRIBUTING.md → `../../CONTRIBUTING.md`. Reused broken-link type but on a mega-repo w/ clean gates. (mendableai/firecrawl 301-renamed to firecrawl/firecrawl.)
- **firecrawl/firecrawl (candidate, verified, NOT PR'd)**: 6 dangling links in skills/firecrawl-build-search/SKILL.md (L50,51,75,76), skills/firecrawl-build/SKILL.md (L92,93), skills/firecrawl-build/references/endpoint-selection.md (L32,33) point at firecrawl-research-index/SKILL.md & firecrawl-developer-index/SKILL.md which DON'T exist under skills/. Likely unpublished skills; no correct target to repoint to → fix = delete refs (maintainer judgment), better as an issue than a PR. Hold.
- **boundaryml/baml (candidate, verified)**: languages/rust/baml-macros/README.md + baml-cli/README.md both `[LICENSE](../../LICENSE)` → languages/LICENSE (missing); root LICENSE is ../../../LICENSE. baml-cli README is also a crates.io-published package (404s on crates.io too). Clear fix BUT CONTRIBUTING bans robot/drive-by PRs → do not target.
- **BerriAI/litellm (candidate, verified)**: litellm/proxy/client/README.md:297,301 `../../CONTRIBUTING.md` & `../../LICENSE` resolve to litellm/litellm/ (missing); correct is ../../../ (root). pip-published README, real 404. Clear fix BUT CLA + mandatory-test rule + internal-only mergers → not worth it for a doc fix.
- **kortix-ai/suna (candidate, verified — MOST re-attemptable)**: packages/sdk/GETTING-STARTED.md:200 `[AGENTS.md](./AGENTS.md)` → packages/sdk/AGENTS.md (missing); text says 'the root AGENTS.md', correct is ../../AGENTS.md (root, exists). npm-published doc, clear fix. Held only for SOC2 PR template + internal-merge culture; genuinely proposable on a future day if we accept the template friction.
- **Hard-gate skips this pass**: OpenHands/software-agent-sdk (accepted-issue + ready-for-dev-label + review-thread-gate).

## 2026-09-03 · suna sweep (B done: kortix-ai/suna#7106 shipped) + trending-repo gate notes
- **B shipped**: kortix-ai/suna#7106 (20.2k stars, daily pushes) — npm-shipped broken doc link: packages/sdk/GETTING-STARTED.md:200 `./AGENTS.md` resolves to packages/sdk/AGENTS.md (404), target is root AGENTS.md (200) -> `../../AGENTS.md`. Was the top backlog candidate from 09-01; gate re-verified (1 non-author review + CI, no accepted-issue/anti-AI gate).
- **ModelEngine-Group/nexent (5.8k stars, develop branch) — candidate w/ CLA friction**: real SDK + docs to mine, but has License/Contributor Agreement (must sign) + GitFlow (PRs target `develop`). Worth revisiting IF we accept CLA; not bug-hunted to line level this pass.
- **Heavy-gate skips (don't spend bullets, verified this pass)**: omnigent (maintainer-approval + security-gate + close-superseded), PraisonAI (claude-merge-gate + parity-gate), deepagents/langchain (require_issue_link + block_fork_main_prs + CLA).
- **Note**: trending AI-agent repos are increasingly bot-gated/CLA-walled; low-gate targets with real verifiable bugs are getting scarce. Non-link bug types (snippet/signature-mismatch) preferred but not always available in low-gate repos — keep the preference soft.
